public class SumSquaredDigits {
    public static void main(String[] args) {
        Kattio io = new Kattio(System.in);
        int p = io.getInt();

        for (int i = 0; i < p; i++) {
            int dataNum = io.getInt();
            int b = io.getInt();
            int n = io.getInt();

            int output = 0;

            System.out.println(dataNum + " " + output);
        }
    }
}